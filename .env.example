# Browserbase (cloud Chrome sessions)
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
BROWSERBASE_CONTEXT_ID=  # Optional: reuse a specific context (auto-created if empty)

# OpenAI (powers Stagehand's page understanding)
OPENAI_API_KEY=

# AgentMail (email for agents)
AGENTMAIL_API_KEY=
AGENTMAIL_INBOX=cursorhack@agentmail.to